apiVersion: v1
kind: Service
metadata:
  name: {{ include "openshift-update-proxy.fullname" . }}
  labels:
    {{- include "openshift-update-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openshift-update-proxy.selectorLabels" . | nindent 4 }}
